# Naive Bayes Theorem:

## The Reason we call it Naive because:
![alt text](image.png)
#### That is we make Naive Assumptions to the Features that they aren't related.
#### IN reality some of these features may be related, but we make the assumption for simplicitiy that the features are not related

### Naive Bayes is used in:
![alt text](image-1.png)

### sklearn.naive_bayes import GaussianNB